// suite test async - disparando excessao

Deno.test("[ fail >> lengthTextMax ] deve retornar msg de excessao maximo ", async () => {
  const input = 'foo';
  const checked = "Ops! o minimo de letras é 100";
  try {
    await makeSut(input);
  } catch (err: any) {
    const output = await err.message;
    if (output !== checked) {
      throw new Error(
        "OPS !!! A MENSAGEM DE ERRO COMPARADA ESTA INCORRETA !!!",
      );
    }
  }
});

// ---
